# 《魔兽世界》："无限" 一键输出、宏与界面工具

> 适用版本：**《魔兽世界》："无限"（World of Warcraft: Forever）**。
> 采集时间：**2026-09-19**（国际服 Beta 第 3 天）。
> 可信度标注：**【官方确认】** = 开发者问答 / 官方面谈明确表述；**【第三方实测】** = 攻略站依据 Beta 与正式服同代限制整理；**【机制事实】** = 暴雪自 2006 年起从未放开、跨版本稳定的系统约束。

---

## 零、直接回答：真·"一键输出"做不到，而且不是 Forever 才做不到

**先把结论说死**：

| 你想要的东西 | 能不能做到 | 原因 |
|---|---|---|
| **点一下，自动按最优循环打完整个战斗** | ❌ **做不到** | 暴雪的宏系统**没有变量、没有循环、没有 if 判断**，这是 2006 年"安全执行（Secure Execution）"改造后就封死的，**与版本无关** |
| **点一下，施放"当前优先级最高且可用"的那个技能** | ✅ **能做到**（最接近的形态） | `/cast` 多技能排队会跳过不可用的 |
| **插件告诉你"下一个该按什么"** | ⚠️ **被严格限制** | Forever 采用**现代（Midnight）战斗 API 限制** + **秘密值（secret values）** 机制 |
| **外部软件 / 硬件宏替你连续按键** | 🚫 **违规，会封号** | 属于自动化，暴雪明令禁止 |

> **一句话**：Forever 里你能得到的最好结果，是**"一个键 = 施放优先级最高的可用技能"**，外加**官方内置的伤害统计、冷却管理器与即将到来的挥砍计时器**。**任何声称"一键自动循环"的方案，要么是骗人的，要么会赔上你的账号。**

---

## 一、宏系统的四条硬限制（20 年没变）

这一节的结论**跨所有版本稳定**，包括 Forever【机制事实】。

| 限制 | 具体表现 | 后果 |
|---|---|---|
| **① 没有变量与循环** | 宏里不能存值、不能计数、不能 while/for | **无法写"循环"** —— 这是"一键循环"根本不存在的根本原因 |
| **② 没有 if / then 分支** | 只能使用**静态可解析**的 `[条件]` 前缀（目标、形态、姿态、战斗状态、修饰键等），且**条件在施法前一次性求值** | **无法根据"这个 buff 还剩 3 秒"来决策** |
| **③ 一次按键受公共冷却约束** | 每成功施放一个技能就进入 GCD | **宏不会替你压缩时间**，连点也不能突破 GCD |
| **④ 宏读不到战斗数据** | 不能读取敌方施法、不能读取战斗日志事件、不能读取冷却剩余时间做分支 | **无法做"智能优先级"** |

### 由此推出的关键行为（决定你该怎么写宏）

**`/cast` 多技能 = 优先级列表，不是循环。** 系统会**从上往下尝试，施放第一个能用的**（不在冷却、资源够、条件满足）。这**不是**循环，每次按键都是**一次全新的从上往下扫描**。

```lua
#showtooltip
/startattack
/cast 技能A
/cast 技能B
/cast 技能C
```
行为：A 可用就放 A（B、C 因 GCD 不执行）；A 不可用则试 B；B 不可用则试 C。**这就是 WoW 里"一键宏"的真实形态。**

---

## 二、三种合法形态（这就是上限了）

### 形态 1：优先级兜底宏（最实用，练级首选）

```lua
#showtooltip
/startattack
/cast 嗜血
/cast 旋风斩
/cast 英勇打击
```
- **用途**：练级、五人本、日常。**点一下 = 打当前最优可用技能**。
- **优点**：不浪费 GCD、不会空放、单手可操作。
- **缺点**：**没有"什么时候该续 DOT / 该不该开爆发"的判断**——这部分永远需要你本人。

### 形态 2：顺序宏（`/castsequence`）

```lua
#showtooltip
/castsequence reset=combat/target 技能A, 技能B, 技能C
```
- **`reset=` 只能按"时间 / 换目标 / 脱离战斗"重置**，**不能按战斗状态重置**。
- **更适合**：起手固定序列（如"标记 → DOT → 主输出"）或**治疗的多级法术**（低等级技能救急）。
- **⚠️ 练级期反而降低效率**：野外战况多变，固定顺序会卡在"技能 B 用不上"的死循环里。**优先用形态 1。**

### 形态 3：条件宏（一键两用，节省动作条）

```lua
#showtooltip
/cast [mod:shift] 技能B; 技能A          -- 平时A，按Shift放B
/cast [harm] 技能X                      -- 目标敌对才放
/use [target=focus] 技能Y               -- 对焦点目标使用
/cast [@mouseover,help] 治疗技能        -- 治疗用：鼠标悬停目标
```
- **用途**：把两三个功能压进一个键位；**治疗职业几乎必用 `[@mouseover,help]`**。
- **注意**：条件是**静态白名单**，不是表达式。**写不出"如果法力低于 30%"这类判断**。

### 常用零碎但收益高的写法

| 写法 | 用途 |
|---|---|
| `/startattack` | **近战第一行必备**：防止平砍中断（换目标、技能失败都会断平砍） |
| `#showtooltip` | 让图标与提示随宏内容变化 |
| `/use 13` `/use 14` | 使用饰品（**两件饰品可写进同一个宏，按可用性依次触发**） |
| `/cast [nomod] A; [mod:alt] B` | 同键两用 |
| `/targetenemy` / `/cleartarget` | 清理目标选择 |

> **附魔/饰品这类"主动性增益"建议单列一个键**，而不是塞进输出宏——因为你需要决定**什么时候**开，而不是让它一到 CD 就自动开。

---

## 三、Forever 的额外限制：连"插件提示"也被堵了

**这是 Forever 与经典怀旧服最大的不同，也是必须先搞清的前提。**

### 3.1 官方原话：用的是"现代 API"【官方确认】

首席软件工程师 **Nora Mills** 在开发者问答中，被直接问到"Forever 用的是 Midnight 的插件限制、经典 API，还是别的"时回答：

> **"我们选择了现代 API 方案。"**

她给了**三条理由**与**两条承诺**：

| 类别 | 内容 |
|---|---|
| 理由① | 对插件开发者有同理心——**现代 WoW 与 Forever 共用一套 API 更容易维护** |
| 理由② | Forever 的首领战**比现代版简化得多**，所以战斗限制**不该负面影响体验** |
| 理由③ | **不希望玩家"觉得必须装插件才能玩"**——那会导致门槛与"守门" |
| 承诺① | **随游戏内置伤害统计 + 冷却管理器** |
| 承诺② | **"挥砍计时器（Swing Timer）可能即将推出"** |

另据官方 UI 频道：**Forever 与主分支共用界面架构，包含 12.1.5 的绝大部分 API**；**12.1.0 引入的 AuraContainer / AuraButton 改动同样可用**；**Midnight 的插件限制（含"秘密值"）在 Forever 同样生效**。

### 3.2 "秘密值（secret values）"是什么意思

这是理解一切的关键【第三方实测，机制描述准确】：

> **插件被允许"显示那个盒子、调整它的大小、把它挪个位置"，但被禁止"打开盒子、用里面的内容做决定"。**

落到实操上：

| 能做 | 不能做 |
|---|---|
| 显示**你自己的** buff / debuff / 冷却 / 资源 | **读取敌方施法条**来判断"该打断了" |
| 显示**你自己**的持续时间与层数 | 读取**战斗日志事件**来推算接下来的最优动作 |
| UI 定制（动作条、框体、字体、背包、地图、拍卖行） | 读取**战斗中其他玩家的状态**来发指令 |
| 首领模组的基础计时（DBM / BigWigs 在正式服同限制下可运行） | **"计算并给出结论"** 类功能——被明确限制 |

**直接后果**：
- **`Hekili` 式"下一个技能"提示按原方式做不出来**——它需要读取冷却/资源/敌方状态去跑优先级，**属于被限制的"计算"类别**。
- **`WeakAuras` 仍然存在**，但**只能做"关于你自己的"那部分**（buff、冷却、资源）。
- **Classic / Era 插件不能直接加载** → 需要 **Forever 专用版本**（Questie、Bagnon、拍卖行、大型 UI 套件已有作者在移植）。
- **`Details` / `Recount` 变成可选**——因为**官方内置伤害统计**才是人人都有的那一个。

### 3.3 官方为什么这么做（理解它，就不会走弯路）

官方口径非常一致：**"插件应该帮你看见游戏，而不是替你玩一部分游戏。"** 再加上 Forever 刻意追求"**没有被解完的艾泽拉斯**"——**限制计算型插件，正是为了让首领机制在第一周保持未知**。

---

## 四、官方给你的三条替代路径

既然宏做不了循环、插件又不能替你算，Forever 把原本靠外部工具补齐的部分**做进了游戏里**：

| 内置功能 | 状态 | 对谁最重要 |
|---|---|---|
| **伤害统计** | **已确认，随游戏发布** | 所有人——**别再为装哪个统计插件纠结**，官方那个是人人都有 |
| **冷却管理器** | **已确认**（此前误记为"计划中"，已更正） | 所有需要卡爆发窗口的职业 |
| **挥砍计时器** | **"即将推出"**（Mills 原话） | **依赖自动攻击节奏的职业**：圣印舞骑士、增强萨 |
| **天赋层面的"去插件化"** | 已实装 | **惩戒骑**：圣印舞改为天赋**「光之交织」**，**不再需要计时器插件**；**增强萨**：漩涡武器把闪电箭节奏简化 |

> **最值得注意的一点**：官方在**用天赋和内置 UI 吃掉原本需要宏与插件的需求**。这意味着你的"效率工具"策略应该从"找插件/写宏"转向"**读透职业机制 + 用官方内置件**"。

---

## 五、合规红线（这一节请务必看完）

| 做法 | 判定 | 后果 |
|---|---|---|
| 游戏内宏（`/cast` / `/castsequence` / 条件宏） | ✅ **官方支持** | 安全 |
| 游戏内合法插件（UI 定制、背包、任务、地图、拍卖行） | ✅ **官方支持** | 安全 |
| **AutoHotkey / 按键精灵等外部连点或循环脚本** | 🚫 **违规** | **账号处罚** |
| **鼠标 / 键盘自带的硬件宏做多键合一或连发** | 🚫 **违规**（暴雪视同外部自动化） | **账号处罚** |
| **读屏 / 读内存来驱动决策的"智能宏"** | 🚫 **严重违规** | **账号处罚** |
| **绕开插件限制的规避手段** | 🚫 **被逐案严查** | **账号处罚** |

**判断标准很简单**：**"这一个按键有没有替你做了本该由你做的判断或操作？"** 有 → 违规。宏只做"按你指定顺序尝试施法"，**判断始终在你**，就安全。

> ⚠️ 顺便提醒：**代练、金币交易、账号共享**同样在禁止之列。Forever **没有代币、没有直升**，官方对经济与账号行为的监管只会更严。

---

## 六、按场景给你的实际建议

### 6.1 练级（1-60）
- **一个优先级兜底宏**（形态 1）+ **`/startattack`** 就是全部所需。
- **别写 `/castsequence` 做输出循环**——野外战况多变，顺序宏会卡死。
- **给"应急"单独立键**：保命技能、解控、绷带/食物**不要塞进输出宏**（那会在你不需要的时候被消耗）。

### 6.2 五人本
- 优先级宏 + **主动增益单独立键**（饰品、爆发）。
- **控制技能（变形/放逐/恐惧/定身）手动按**——Forever **明确保留控场价值**，自动化的宏无法判断"该不该控"。
- 内置伤害统计够你复盘，不用急着找插件。

### 6.3 团本（12-09 起）
- **推进度靠人，不靠工具**。官方**首发没有地下城手册、进团前不知道首领**，且**取消了团本 PTR 测试** —— **第一周没有任何工具能提前告诉你该怎么做**。
- 把精力放在**"读战斗"**上：仇恨、控场、集火目标、抗性与生物类型克制（**带 2-3 套装备**）。
- **DBM / BigWigs 能跑**，但**能播报多少取决于首领暴露了什么** —— 不要当成默认保障。

### 6.4 如果你想最大化操作效率
1. **用条件宏压缩键位**（`[mod:]`、`[@mouseover,help]`），把功能密度提上去。
2. **用内置冷却管理器 + 挥砍计时器**替代过去的插件依赖。
3. **把"判断"留在自己手里** —— 这是 Forever 刻意设计的方向，也是它和正式服手感最大的不同。
4. **`/startattack` 放在每一个近战宏的第一行**（微小但累积收益最大的一条）。

---

## 七、待验证清单

- [ ] Forever **宏语法的具体支持范围**是否与现代客户端完全一致（推测一致，因共用界面架构）
- [ ] **挥砍计时器**的实际上线时间与形态
- [ ] **内置伤害统计**能否记录团队全员、能否导出
- [ ] **内置冷却管理器**是否支持自定义（多层充能、条件显示）
- [ ] `WeakAuras` 的 **Forever 专用版本**实际能做什么 / 不能做什么（作者未发布）
- [ ] `DBM` / `BigWigs` 在 Forever 上的**实际播报能力**（取决于首领暴露的数据）
- [ ] 官方是否会公布**完整的 Forever 插件 API 规则文档**（Mills 称"上线前会分享更多技术细节"）
- [ ] `Hekili` 类工具是否**完全无法存在**，或能以"允许范围内"的形式重构

---

> 本文件由人工触发更新（未启用自动监控）。需要拉取最新情报时，见 `update-playbook.md`。
> 相关文件：`stats-and-gear.md`（属性与配装）、`talents-and-gear.md`（天赋配点）、`beta-findings.md`（外服实测与捷径）、`efficiency-guide.md`（效率指南）。
